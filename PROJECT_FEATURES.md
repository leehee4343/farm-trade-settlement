# 농장과의 거래/정산 서비스 - 기능 정리

현재까지 만들어진 프로토타입(`index.html`, GitHub Pages 배포됨)을 기준으로 정리한 전체 기능 문서입니다.

- 배포 URL: https://leehee4343.github.io/farm-trade-settlement/
- 저장소: https://github.com/leehee4343/farm-trade-settlement

---

## 1. 서비스 개요

**식육포장처리업체가 농장으로부터 돼지(생돈)를 매입하는 거래를 정산·관리하는 시스템**입니다. 흐름은 크게 "거래 발생 → 개체별 등급/중량 데이터 확인 → 단가·지급율 적용해 정산금액 계산 → 확정 → 이력 조회/사후관리"로 이어집니다.

좌측 사이드바에는 **농장정보 관리**와 **거래내역 조회** 두 개의 메뉴만 있고, 나머지 화면들은 그 안에서 맥락에 따라 진입하는 하위 화면 구조입니다.

---

## 2. 화면별 기능

### 📂 거래내역 조회 (기본 진입 화면)

- 농가별(셀렉트박스) + **거래일** 기간으로 검색되는 목록 (14건의 모의 거래 데이터, A~J 10개 농가)
- 컬럼: 거래일 / 전표번호(`TRD-YYYYMMDD-####` 형식, 클릭 가능한 링크) / 농가명 / 이력번호 / 총거래두수 / 차인지급액 / 상태(확정·대기) / **최종수정일시**
- 하단에 페이지네이션(«‹1~10›»), '+ 거래명세서 작성' 버튼(신규 작성 진입)
- 목록 자체에는 수정/삭제 버튼이 없고, **전표번호를 클릭하면 상세화면으로 이동**하는 구조

### 🧾 거래명세서 작성 (핵심 화면 — 3가지 모드로 동작)

같은 화면이 상황에 따라 다르게 동작합니다.

**신규 작성 모드**
- 거래일 선택 → '거래 찾기'로 그 날짜의 거래 건(농가) 조회/선택
- 생체중계·적용단가·지급율 입력
- 개체별 내역(성별/등급/생체중/지육중/등지방/기본금액/공제 등) 확인, 생체중 셀은 직접 수정 가능(수정 시 기본금액 자동 재계산)
- 평균정보·등급판정(1+/1/2/등외, AB출현율) 자동 집계
- 하단 버튼: 취소 / 저장(임시저장) / **정산완료(확정)**

**상세보기(읽기전용) 모드**
- 목록에서 전표번호 클릭 시 진입
- 모든 입력 필드가 비활성화되고 텍스트로만 표시(거래일은 특히 크고 굵게 강조)
- 전표번호·최종수정일시가 별도 표시됨
- 하단 버튼: 엑셀 다운로드 / 인쇄 / 삭제 / **확정 취소** / 수정

**수정 모드**
- 상세보기에서 '수정' 클릭 시 진입
- **거래일/농가/전표번호 같은 신원 정보는 고정**한 채 적용단가·지급율·개체별 생체중 등 실제 정산 내용만 다시 편집 가능
- 저장 시 최종수정일시 갱신, 취소 시 원본 데이터로 롤백

**계산식**
```
기본금액   = 생체중 × 적용단가 × 지급율/100
정산액     = Σ기본금액 - (중량공제 + 등지방공제 + 기타공제)
차인지급액 = 정산액 + 장려금 - 공제액 - 자조금외
```

**부가 기능**
- 인쇄: 버튼/네비게이션을 숨기고 본문만 출력
- 엑셀 다운로드: 전표번호·요약·평균정보·등급판정표·개체별 내역까지 화면에 보이는 전체를 `.xls`로 내려받음

### 🏠 농장정보 관리

- 셀렉트박스(검색대상: 전체/농장식별번호/농장명/사업자등록번호/대표자) + 키워드 + 검색 버튼
- 목록: 농장식별번호 / 농장명 / 사업자등록번호 / 대표자 / 지급율 기준(농장별·거래별) / 기본 지급율(거래별인 농가는 "거래 시 설정"으로 표시) / **거래 건수**(누적 확정 정산 건수, 두수 아님) / 최근 거래일 / '공제/지급율 설정' 버튼
- **'+ 농장정보 가져오기'** 팝업: 돼지 등급판정 시스템에 등록된, 우리 업체와 거래 이력 있는 외부 농장 목록에서 선택해 우리 목록으로 가져오는 기능(이미 등록된 농가는 비활성 표시)

### ⚙️ 공제/지급율 설정 (농장정보 관리에서만 진입하는 하위 화면)

- 지급율 설정 기준(농장별/거래별) + 기본 지급률 입력
- **'인센티브/공제 설정'** — 실제 자료("원료돈 출하조건" 표)를 반영한 카테고리별 설정
  - 규격미달 / 규격초과 (각 3단계, 범위는 `[ ]~[ ]` 2칸 입력)
  - 등지방
  - 비거세돈
  - 화농공제 / 이상육
  - 성비패널티
  - 지육율
  - 계류장 사돈
  - 각 항목: 토글(적용 여부) + 범위 입력 + 공제/인상 금액 입력

---

## 3. 정산 확정 시 연동되는 데이터 흐름

'정산완료(확정)'를 누르면:
- 거래내역 목록에 새 행 추가(전표번호 자동채번)
- 해당 농가의 '거래 건수' +1, '최근 거래일' 갱신

반대로 상세화면에서 '확정 취소'를 하면:
- 상태가 '대기'로 변경
- 해당 농가의 거래 건수 -1

---

## 4. 프로토타입 특성

- 순수 정적 HTML/CSS/JS 단일 파일(백엔드 없음), 모든 데이터는 JS 배열에 하드코딩된 모의 데이터
- 반응형(모바일에서 사이드바가 오프캔버스 드로어로 전환)
- URL 해시 기반 라우팅(새로고침해도 화면 유지)
- 실제 '돼지 등급판정 시스템' / '돼지거래가격 조사시스템' 연동은 없고 toast 메시지로 시뮬레이션
